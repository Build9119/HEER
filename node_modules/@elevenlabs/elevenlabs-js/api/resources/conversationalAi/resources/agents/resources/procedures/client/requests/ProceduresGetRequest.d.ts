/**
 * @example
 *     {
 *         versionId: "version_id"
 *     }
 */
export interface ProceduresGetRequest {
    /** The version ID to retrieve. If omitted, returns the version at branch HEAD. */
    versionId?: string;
}
